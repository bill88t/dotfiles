#!/usr/bin/env python3
import sys

# Read all lines from stdin and strip newlines
lines = [line.strip() for line in sys.stdin if line.strip()]

# If there are any lines, output the rm command
if lines:
    print('rm -v ' + ' '.join(f'"{line}"' for line in lines))
