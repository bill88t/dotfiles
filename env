#!/bin/bash

if [[ "$*" == "DESKTOPINTEGRATION=1 AyuGram --"* ]]; then
    exec /usr/bin/env "$@"
fi

if [[ -z "${SECRETS_LOADED}" || "${SECRETS_LOADED}" != "1" ]]; then
    exec /usr/bin/env "$@"
else
    echo "ERROR: Secrets are loaded. Env restricted." >&2
    exit 1
fi
